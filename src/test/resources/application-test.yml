spring:
  datasource:
    url: jdbc:h2:mem:async-align;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
  flyway:
    enabled: true
  jpa:
    hibernate:
      ddl-auto: validate

app:
  jwt:
    secret: test-secret-for-jwt-signing-must-be-at-least-32-bytes
